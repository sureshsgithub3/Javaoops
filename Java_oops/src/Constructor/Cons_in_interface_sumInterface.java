package Constructor;

public interface Cons_in_interface_sumInterface {
	/*
	 * interface cannot have constructors.
	 * All the methods of interface doesn’t have body so there is no need to call the methods in the interface itself.
	 *  In order to call any method we need an object since there is no need to have object of interface, there is no need of having constructor in interface .
	 *  
	 * 
	 * 
	 * 
	
	
	
	*/
	/*Cons_in_interfacesum(){
		
	}
	*/
	
	
	
	public void sum();

}
